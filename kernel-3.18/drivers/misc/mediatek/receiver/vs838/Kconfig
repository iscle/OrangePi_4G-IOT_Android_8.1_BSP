config MTK_RECEIVER_VS838
	bool "MTK_RECEIVER_VS838"
	default n
	help
	  It support different receiver sensor.
          If this option is set,
	  it will support
          MTK_RECEIVER_VS838 sensor.
